# 本次项目改动总结（2026-03-03）

## 1. 本次主要改动工作
围绕 PX4 v1.16 + QGC 5.0.8：
- 文档编写：完善控制算法与 EKF2 章节。
- 固件裁剪：检查固件裁剪，确保保留了多旋翼/固定翼/复合翼的相关模块。

---

## 2. 已完成改动

### 2.1 文档内容完善
主要完善了主文档中的“飞控算法”和“感知导航（EKF2）”部分：
- 飞控控制部分结构按：角速度环 → 姿态环 → 位置环 → 控制分配。
- 每个控制环统一为三段式：
  1) 参数
  2) 主循环
  3) 核心算法函数
- 新增 EKF2 章节，覆盖：
  - 参数分组（GNSS/磁罗盘/高度源/光流/多实例等）
  - 主循环（采样、入队、预测、融合、发布）
  - 核心函数（预测、融合调度、观测延迟补偿）

### 2.2 固件裁剪调整
在default.px4board文件中上，开启了复合翼相关模块：
```
CONFIG_MODE_NAVIGATOR_VTOL_TAKEOFF=y     #开启VTOL(复合翼)的相关模块
CONFIG_MODULES_VTOL_ATT_CONTROL=y        #开启VTOL(复合翼)的相关模块
```

### 2.3 编译后固件占用
在开启复合翼相关模块后，编译的固件占用如下。可以看出，当前固件下FLASH占用很高，后续可以考虑进一步裁剪：
```
Memory region         Used Size  Region Size  %age Used
        ITCM_RAM:          0 GB        64 KB      0.00%
           FLASH:     1916420 B      1920 KB     97.47%
       DTCM1_RAM:          0 GB        64 KB      0.00%
       DTCM2_RAM:          0 GB        64 KB      0.00%
        AXI_SRAM:      100796 B       512 KB     19.23%
           SRAM1:          0 GB       128 KB      0.00%
           SRAM2:          0 GB       128 KB      0.00%
           SRAM3:          0 GB        32 KB      0.00%
           SRAM4:          2 KB        64 KB      3.12%
          BKPRAM:          0 GB         4 KB      0.00%
```
---

## 3. 修改的文件
- 文档更新：README.md
- v6x固件配置：boards/px4/fmu-v6x/default.px4board

---
